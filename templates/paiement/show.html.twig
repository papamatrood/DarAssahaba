{% extends 'base.html.twig' %}

{% block title %}Paiement{% endblock %}

{% block body %}
    <h1>Paiement</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ paiement.id }}</td>
            </tr>
            <tr>
                <th>Libelle</th>
                <td>{{ paiement.libelle }}</td>
            </tr>
            <tr>
                <th>IsPaye</th>
                <td>{{ paiement.isPaye ? 'Yes' : 'No' }}</td>
            </tr>
            <tr>
                <th>StudentId</th>
                <td>{{ paiement.studentId }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('paiement_index') }}">back to list</a>

    <a href="{{ path('paiement_edit', {'id': paiement.id}) }}">edit</a>

    {{ include('paiement/_delete_form.html.twig') }}
{% endblock %}
